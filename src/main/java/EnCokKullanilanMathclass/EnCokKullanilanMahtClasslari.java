package EnCokKullanilanMathclass;

public class EnCokKullanilanMahtClasslari {
    public static void main(String[] args) {


  /*  Java dilinde Math sınıfının en çok kullanılan metodları şunlardır:

            Math.abs(x) : Verilen tam sayı veya ondalıklı sayı değerinin mutlak değerini verir.

            Math.ceil(x) : Verilen ondalıklı sayı değerinin bir üst tam sayı değerini verir.

            Math.floor(x) : Verilen ondalıklı sayı değerinin bir alt tam sayı değerini verir.

            Math.max(a, b) : İki sayı arasındaki en büyük sayıyı verir.

            Math.min(a, b) : İki sayı arasındaki en küçük sayıyı verir.

            Math.pow(a, b) : 'a' üzeri 'b' değerini verir.

            Math.random() : 0 ve 1 arasında rastgele bir ondalıklı sayı verir.

            Math.round(x) : Verilen ondalıklı sayı değerinin en yakın tam sayı değerini verir.

            Math.sqrt(x) : Verilen sayının karekökünü verir.

    Bu metodlar yalnızca Math sınıfının yalnızca bir kısmını oluşturur, diğer birçok fonksiyon da bulunur.
}*/
    }}